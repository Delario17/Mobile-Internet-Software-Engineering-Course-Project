# Codex 项目开发说明

本文件适用于当前目录及其所有子目录。参与本项目的 Codex 或其他开发代理在阅读、设计、编码、测试和修改配置前，必须遵守以下要求。

## 1. 固定技术栈

- 应用使用华为 **ArkTS** 开发，界面使用 **ArkUI**，应用模型采用 HarmonyOS Stage 模型。
- `compatibleSdkVersion` 固定对应 **HarmonyOS SDK 6.0.0（API 20）**。配置文件中应使用当前工程和 DevEco Studio 所要求的合法写法表达 `6.0.0 (20)`。
- 不得擅自提高或降低 compatible SDK，不得为了使用较新的 API 改变该版本。
- `targetSdkVersion`、产品模型、签名和设备类型等其他配置，应根据工程现状与上架要求处理；没有明确需求时不要顺带修改。
- 代码必须符合 ArkTS 的类型和语法约束。避免依赖 ArkTS 不支持的动态 JavaScript/TypeScript 写法，不用 `any` 绕过类型设计。
- 应用运行时代码不得改用 React Native、Flutter、Web 容器或其他跨端框架代替 ArkTS。

## 2. 需求基线

- 开发项目一前，先阅读 [项目一需求规格说明.md](./项目一需求规格说明.md)。功能设计、数据结构和验收以该文档中的 `P1-FR`、`P1-AC` 和 `P1-NFR` 编号为基线。
- 项目一的核心流程是：完整录制或导入音频 → 端侧语音识别与时间对齐 → 生成可点击的识别句段 → 局部重录 → 非破坏式替换 → 全文预览和本地导出。
- 源录音始终保持不可变。自动分句只建立时间映射；补录通过编辑决策加入成片时间轴。
- 完整讲稿、提纲和专业词均为可选输入。只有音频时，识别、分句、局部重录和导出流程仍应完整可用。

## 3. 华为官方 API 优先

所有系统能力优先使用华为官方公开 API、SDK、组件和示例，选择顺序如下：

1. compatible SDK 6.0.0（API 20）中可用的 HarmonyOS 公共 API；
2. 华为官方公开的小艺端侧能力或 HarmonyOS AI 能力；
3. 华为官方提供的 SDK、Sample、Codelab 或推荐实现；
4. 项目内部的薄封装与适配层；
5. 在公开调用方式尚未确认时，搭建可编译框架并留下规范的 TODO。

调用官方 API 前必须从华为官方开发者文档确认以下内容：

- 包名、类名、方法签名与返回类型；
- API 起始版本，确认 API 20 可用；
- 所需权限、权限申请时机和拒绝后的行为；
- 设备型号、区域、系统版本或账号等限制；
- 前后台生命周期、并发、线程与资源释放要求；
- 错误码、取消方式和恢复策略；
- 应用市场上架、模型权重和组件许可证要求。

不得凭印象虚构华为 API、包名、系统能力或错误码。无法从官方资料确认的调用不得伪装成已经接通。

## 4. 小艺端侧模型要求

- 端侧语音识别、语义理解或图像理解需要模型能力时，优先调用华为官方提供的 **小艺端侧大模型能力**。
- 音频、图片、识别文本、模型输入、中间结果和输出均应留在设备本地。核心流程不得依赖云端推理。
- 未经用户明确同意，不得替换为第三方云端模型、第三方在线语音识别或自建网络接口。
- 不得在正式功能中用硬编码文本、随机结果或静态 JSON 冒充小艺推理结果。

项目一接入小艺时，需要的官方能力至少包括：

- 对本地完整音频执行端侧普通话语音识别；
- 返回识别文字及词级、短语级或可用于分句的时间戳；
- 返回置信度或等价的不确定性信息；
- 支持处理进度、取消和明确错误状态；
- 在飞行模式下完成识别与时间对齐。

如果小艺公开 API 无法直接满足某项需求，应保留独立适配层，使未来能够替换具体调用，而不让 UI、项目存储或编辑时间轴依赖某个未经确认的接口。

## 5. 官方调用尚未确认时的处理方式

如果不确定某项华为官方 API 或小艺能力如何调用，继续完成可确定的工程结构，不要停在概念描述，也不要编造调用代码。至少完成以下内容：

1. 定义清晰的 ArkTS 接口、请求类型、结果类型和错误类型；
2. 建立官方能力适配器及依赖注入位置；
3. 接通 UI 的加载、进度、取消、成功、失败和重试状态；
4. 接通项目数据保存与恢复流程；
5. 在唯一的实际调用位置留下 `TODO(HUAWEI_API)`；
6. 未接通时返回明确的“能力尚未接入”错误，不能返回伪造成功结果；
7. 在本次交付说明中列出全部未完成的官方 API 接入点。

TODO 使用以下格式：

```ts
// TODO(HUAWEI_API): 接入华为官方小艺端侧语音识别 API。
// 需要的能力：输入本地音频或 PCM 数据，返回带起止时间戳和置信度的识别结果。
// 运行约束：HarmonyOS SDK 6.0.0（API 20）、完全离线、支持进度、取消和错误状态。
// 接入前确认：官方包名、方法签名、权限、设备限制、生命周期、错误码和发布许可。
// 官方文档：确认后在此填写链接。
```

TODO 必须说明“需要调用什么官方能力、输入是什么、预期输出是什么、适用 SDK、仍需确认什么”。只写“以后接 API”不合格。

## 6. 项目一建议的能力边界

以下名称表示项目内部职责，不代表华为官方 API 名称。实现时可以根据工程结构调整命名，但职责应保持分离：

| 内部能力 | 职责 | 优先使用的官方能力 |
| --- | --- | --- |
| `AudioCaptureService` | 完整录音、暂停、恢复、结束、录音中断恢复 | HarmonyOS 官方音频录制 API |
| `LocalAudioImportService` | 通过系统选择器导入本地音频并校验格式 | HarmonyOS 官方文件选择与媒体解码 API |
| `XiaoyiSpeechRecognitionAdapter` | 端侧 ASR、时间对齐、置信度、进度与取消 | 华为小艺官方端侧语音能力 |
| `TranscriptSegmentService` | 根据时间戳、停顿和标点生成及修订识别句段 | ArkTS 本地业务逻辑 |
| `ReplacementTakeService` | 针对目标句段录制、保存和选择多个替换版本 | HarmonyOS 官方音频录制 API |
| `EditDecisionStore` | 保存当前采用版本、边界、停顿和淡入淡出参数 | HarmonyOS 官方本地持久化能力 |
| `AudioRenderService` | 按编辑决策预览并导出完整音频 | HarmonyOS 官方音频解码、处理与编码 API |

华为官方 API 应集中封装在适配器内。页面和业务对象不得到处直接依赖系统 API，避免 API 调整时修改整条业务链。

## 7. 编码要求

- 使用明确的数据类型表达项目、源录音、识别词元、识别句段、替换版本和编辑决策。
- 异步调用必须处理成功、失败、取消、超时和生命周期中断。
- 资源使用结束后及时释放录音器、解码器、文件句柄和模型会话。
- 不吞掉异常，不把失败转换为看似成功的空结果。
- 日志不得打印完整录音路径、转写全文、用户提纲或其他隐私数据。
- 权限只在功能真正需要时申请，并提供拒绝、永久拒绝和系统中断后的明确状态。
- 原始素材与派生文件分开保存；任何编辑都不得覆盖用户选择的原文件。
- 项目保存采用可恢复写入方式，临时文件与已确认结果分开。
- 新增依赖前检查华为官方能力是否已经覆盖；依赖必须记录用途、版本和许可证。
- UI 文字、状态和错误提示使用统一资源管理，避免散落硬编码。

## 8. 验证要求

每次实现或修改功能后，根据改动范围完成以下验证：

1. ArkTS 编译与工程静态检查通过；
2. compatible SDK 仍为 6.0.0（API 20）；
3. 在参考鸿蒙设备或官方模拟环境中运行相关流程；
4. 飞行模式下验证核心功能；
5. 验证权限拒绝、录音中断、识别失败、存储不足和应用重启恢复；
6. 验证源录音没有被修改；
7. 验证全文预览与导出使用相同编辑决策；
8. 检查正式路径中没有 Mock、硬编码模型结果或静默降级到网络服务；
9. 汇总 `TODO(HUAWEI_API)`，说明已完成的框架、缺少的官方调用和下一步所需资料。

## 9. 文档与提交说明

- 实现功能时，在代码或开发文档中关联对应的需求编号，例如 `P1-FR-006`。
- 接入华为官方能力后，记录官方文档链接、API 名称、起始版本、所需权限和已验证设备。
- 改变功能范围、数据结构或验收标准时，同步更新对应需求规格说明。
- 提交说明应写明：完成的需求、使用的华为官方 API、实机验证结果、遗留 TODO 和已知限制。
- 发现官方 API 在 API 20 不可用时，保留已完成的适配框架和 TODO，并明确报告版本冲突，不得擅自升级 compatible SDK。
