# 项目一需求规格说明

文档版本：V1.1
文档状态：立项需求稿（已确认低置信度人工审核门槛）
编制日期：2026-09-22
产品名称：AudioPatch——基于转写文本的局部重录应用  

## 1. 文档说明

### 1.1 编写目的

本文规定项目一的产品范围、用户流程、功能需求、数据结构、异常处理、非功能要求和验收标准，作为原型设计、端侧语音模型选型、开发、测试及参赛材料编写的共同依据。

### 1.2 产品范围

项目一允许用户先连续录完一整段音频，或导入已有音频。应用在本机完成语音识别、时间对齐和自动分句；用户通过识别文字定位目标句段，录制替换版本，并导出更新后的完整音频。

讲稿、提纲和专业词均为可选输入。仅有音频时，完整流程仍可使用。

### 1.3 需求优先级

| 优先级 | 含义 |
| --- | --- |
| P0 | 首版必须完成，缺失时核心流程无法成立 |
| P1 | 首版重要能力，资源受限时可在核心流程跑通后完成 |
| P2 | 后续扩展，不作为首版交付条件 |

### 1.4 术语

| 术语 | 定义 |
| --- | --- |
| 导入 | 从手机录音或文件中选择本地音频，数据不发送至服务器 |
| 端侧 ASR | 在手机本地完成的语音识别与时间对齐 |
| 源录音 | 用户第一次完整录制或导入的原始音频，项目编辑过程中保持不可变 |
| 识别句段 | 端侧 ASR 生成的、带开始和结束时间戳的可编辑文本单元 |
| 替换录音 | 用户针对某个识别句段重新录制的候选音频 |
| 当前采用版本 | 某个句段在全文预览和导出时实际使用的源录音或替换录音 |
| 成片时间轴 | 按顺序组合所有当前采用版本后形成的音频序列 |
| 非破坏式替换 | 通过编辑决策改变成片时间轴，源录音保持不变并可随时恢复 |

### 1.5 运行环境与权限

应用运行于具备麦克风、本地文件访问和端侧模型推理能力的鸿蒙手机。正式开发前确定参赛演示机和最低配置测试机，并记录操作系统、CPU/NPU、内存、存储空间及模型版本。

| 权限或能力 | 必要性 | 使用目的 |
| --- | --- | --- |
| 麦克风 | 必需 | 完整录音和局部重录 |
| 用户选择的本地文件 | 必需 | 导入已有音频及导出成片 |
| 应用私有存储 | 必需 | 保存源录音副本、转写结果、替换版本和编辑状态 |
| 网络 | 无需 | 正式版本的录音、识别、重录、预览与导出均在本机完成 |

## 2. 产品需求

### 2.1 产品目标

项目一解决“完整录音中只有一句需要修改，却必须反复回听、寻找波形位置并手工剪接”的问题。

用户默认先连续录完一整段内容，也可以导入已有音频。完整讲稿不是使用前提：用户可以不提供任何文字，也可以只输入提纲或专业词汇。应用根据实际音频在端侧生成带时间戳的转写文本并自动分句。用户通过文字找到目标句段，录制一个或多个替换版本，选择满意版本后预览和导出完整音频。

核心机制是“音频—文本—时间轴双向绑定”：点击文字能够播放对应声音，播放声音时同步突出当前文字；局部重录只改变成片时间轴的选择，源录音始终保留。

### 2.2 目标用户

- 录制竞赛作品介绍的学生团队；
- 制作课程讲解、社团宣传和视频旁白的学生；
- 需要在手机上完成短篇真人配音的小型内容创作者。

首版聚焦普通话、单说话人、以语音为主的短音频。

### 2.3 典型用户故事

#### P1-US-01 完整录完后修改一句

用户连续录制三分钟作品介绍，回听时发现中间一句专业术语读错。用户希望点击识别出的文字，直接补录该句，同时保留其余已经满意的内容。

#### P1-US-02 只有音频，没有完整讲稿

用户依据几个提纲自由讲述并完成录音。应用直接从音频生成带时间戳的文本和句段，用户无需补写完整讲稿即可定位与重录。

#### P1-US-03 导入已经录好的音频

用户从本地导入一段已有旁白。应用完成端侧识别和分句，用户修改两个句段后导出新版本，源文件保持不变。

#### P1-US-04 修正识别错误

端侧模型把两个短句合成了一句，并识错一个专业词。用户修改显示文字、拆分句段并试听边界，再对其中一句进行补录。

#### P1-US-05 比较多个补录版本

用户对同一句录制三个版本，通过“上一句—补录句—下一句”的连续试听比较语气，最终选择最自然的版本；之后仍能恢复源录音。

### 2.4 核心流程

```mermaid
flowchart LR
    A[完整录制或导入音频] --> B[可选：输入提纲或专业词]
    B --> C[端侧语音识别与时间对齐]
    C --> D[生成带时间戳的识别句段]
    D --> E[点选文字并试听]
    E --> F[录制目标句的新版本]
    F --> G[前后文连续试听并选版]
    G --> H[全文预览]
    H --> I[本地导出完整音频]
```

### 2.5 功能需求

| 编号 | 优先级 | 需求说明 | 验收要点 |
| --- | --- | --- | --- |
| P1-FR-001 | P0 | 用户可以新建项目并选择“完整录制”或“导入音频”。 | 两种入口均能进入端侧识别流程。 |
| P1-FR-002 | P0 | 完整录制采用连续录音方式，提供开始、暂停、继续、结束和时长显示。录制过程中无需按句暂停。 | 结束后生成一条可播放的源录音。 |
| P1-FR-003 | P0 | 用户可以导入首版支持格式的本地音频。系统在识别前校验格式、音轨、文件完整性、时长和可用空间。 | 损坏或无音轨文件不会进入伪成功状态。 |
| P1-FR-004 | P0 | 提纲、完整讲稿和专业词均为可选输入。未提供文字时，录制、识别、重录和导出流程仍可完成。 | 空文本项目能够完成端到端操作。 |
| P1-FR-005 | P1 | 可选提纲在录制时作为参考显示；其中的专业词可作为本地识别的辅助词汇。识别文本仍以实际音频为准。 | 提纲与口述不完全一致时不会阻断识别。 |
| P1-FR-006 | P0 | 应用在端侧完成语音活动检测、语音识别和时间对齐，结果至少包含文字、词级或短语级时间戳及置信度。 | 飞行模式下可完成识别。 |
| P1-FR-007 | P0 | 应用综合时间戳、停顿和标点生成按时间排序的识别句段。每个句段具有稳定 ID、显示文字、源录音起止时间和置信度。 | 相邻句段顺序与源录音一致。 |
| P1-FR-008 | P0 | 用户点击识别句段可以播放其对应音频；全文播放时界面同步突出当前句段。 | 文本与音频可以双向定位。 |
| P1-FR-009 | P0 | 用户可以修改识别文字、合并相邻句段、拆分句段并调整起止边界。仅修改文字不会改变音频内容。 | 修改后可立即试听对应范围。 |
| P1-FR-010 | P1 | 低置信度文字和边界以统一样式标记，并可集中跳转检查。低置信度句段必须经用户人工确认文字和边界后，才能作为可发布内容导出；审核前仍可修改、试听和继续编辑。置信度不可用或未配置审核阈值时，按低置信度处理。 | 用户能够逐项核对低置信结果；未完成审核时导出被明确阻止，审核通过且句段版本未变化后才允许导出。 |
| P1-FR-011 | P0 | 用户可以选中一个句段并进入局部重录。录制前可回听上一句末尾，随后倒计时开始录制。上下文回放不写入替换录音。 | 替换文件仅包含本次重录内容。 |
| P1-FR-012 | P0 | 每次局部重录生成一个独立候选版本，并与目标句段关联；替换录音时长可以与原句不同。 | 同一句可以保存和试听多个版本。 |
| P1-FR-013 | P0 | 用户可以在源录音和多个替换录音之间选择当前采用版本，也可以恢复源录音。 | 切换选择后全文预览立即更新。 |
| P1-FR-014 | P0 | 局部替换采用非破坏式编辑：源录音保持不可变，系统以编辑决策记录句段引用和版本选择；后续内容根据替换录音时长自动顺延。 | 替换前后源录音校验值一致，前后内容无漏句或重复。 |
| P1-FR-015 | P0 | 系统提供“前句—当前版本—后句”连续试听，并允许调整句间停顿。 | 连续试听与全文预览采用相同编辑参数。 |
| P1-FR-016 | P1 | 拼接时应用短淡入淡出、静音边界修整和响度匹配，减少爆音、句首截断及音量突变。淡入淡出从版本化 DSP 配置读取，首版默认淡入/淡出各 10 ms，且不超过有效内容时长的一半；用户或产品后续可以调整默认配置。处理参数和配置版本保存在项目中。 | 预览与导出使用同一参数和 `dspProfileVersion`；历史修订不因默认值变化而改变。 |
| P1-FR-017 | P0 | 用户可以全文预览、从任一句段跳转播放，并对比源录音和替换版本。 | 播放序列与成片时间轴一致。 |
| P1-FR-018 | P0 | 用户可以将当前成片时间轴导出为首版约定的通用音频格式。导出生成新文件并保留可编辑项目。 | 导出不会覆盖源录音。 |
| P1-FR-019 | P0 | 录音、识别、文本修订、句段调整、版本选择和停顿设置完成后自动保存。 | 应用重启后恢复上次已确认状态。 |
| P1-FR-020 | P0 | 识别未生成有效句段时，用户仍可播放源录音、重试识别，或在波形上框选连续范围建立手动句段。 | 识别失败不会导致源录音不可用。 |
| P1-FR-021 | P1 | 用户可撤销最近的文字修订、边界调整和版本选择。 | 撤销后预览与数据状态同步恢复。 |

### 2.6 识别句段与局部替换规则

1. 自动分句只建立时间映射，不把源录音永久切碎。
2. 一个识别句段引用源录音中的连续时间范围。
3. 调整文字只改变显示内容；调整边界才改变句段引用范围。
4. 一个替换录音只属于一个句段。
5. 任一时刻，每个句段只有一个当前采用版本。
6. 补录长于或短于原句时，成片时间轴重新计算后续片段位置。
7. 多个相邻句段均被替换时，按照句段顺序依次组合。
8. 全文预览与导出读取同一份编辑决策，保证结果一致。

### 2.7 数据需求

| 数据对象 | 主要字段 |
| --- | --- |
| `AudioProject` | 项目 ID、名称、输入方式、可选提纲、状态、上次位置、数据版本、模型版本、创建时间、更新时间 |
| `SourceAudio` | 本地路径、文件校验值、时长、采样参数、编码格式；创建后只读 |
| `WordToken` | 识别文字、开始毫秒、结束毫秒、置信度 |
| `TranscriptSegment` | 稳定 ID、顺序、显示文字、源录音起止毫秒、置信度、边界来源、人工修订标记 |
| `ReplacementTake` | 版本 ID、目标句段 ID、音频路径、时长、创建时间、质量检测结果 |
| `EditDecision` | 句段 ID、当前采用版本、裁切边界、淡入淡出、响度和停顿参数、`dspProfileVersion` |
| `ExportRecord` | 导出文件、格式、时间、使用的项目编辑版本号 |

项目状态：`新建 → 录制/导入中 → 识别处理中 → 可编辑 → 导出中 → 可编辑`。录制或识别中断后进入“待恢复”状态；已写入且可解码的音频作为待确认草稿保留。

### 2.8 异常处理

| 异常 | 系统行为 |
| --- | --- |
| 麦克风权限未授予或麦克风被占用 | 不开始录制；保留已创建项目和可选提纲，提供授权与重试入口。 |
| 来电、切后台或进程终止打断录制 | 封存已写入且可解码的内容为待确认草稿，用户可试听、继续使用或重新录制。 |
| 导入文件损坏、格式不支持或无音轨 | 显示具体原因并允许重新选择，不创建识别任务。 |
| 存储空间不足 | 在录制、转码和导出前检查；处理中不足时清理临时文件，保留源录音和最近一次有效编辑状态。 |
| 未检测到有效语音 | 保留源录音，提供播放检查、重新识别和手动建立句段入口。 |
| 局部识别置信度低 | 标记对应句段，允许改字、合并、拆分、调整边界和试听核对；在用户确认文字和边界前，禁止生成已发布导出结果。 |
| 识别置信度不可用或审核阈值缺失 | 所有识别句段按待人工审核处理，不得以缺少数值或配置为理由绕过审核；用户确认后才可继续导出。 |
| 替换录音过短、未检测到语音或发生严重削波 | 保存为未采用候选并提示重新录制，当前采用版本保持。 |
| 预览渲染或导出被中断 | 保留编辑决策，未完成的临时输出不计为成功文件，用户可重试。 |
| 替换录音文件缺失 | 句段标记为版本不可用并回退至源录音，提示重新补录。 |

### 2.9 首版验收标准

| 编号 | 验收要求 |
| --- | --- |
| P1-AC-001 | 不提供讲稿或提纲，分别通过完整录制和导入音频创建项目，均能生成可点击、按时间排列的识别句段。 |
| P1-AC-002 | 仅输入若干提纲要点后完成自由讲述；提纲与实际口述存在差异时，仍能依据实际音频完成识别、分句、重录和导出。 |
| P1-AC-003 | 在安静室内普通话测试集上，端侧 ASR 字错误率不高于 15%；报告中注明参考设备、语料、录音距离和环境。 |
| P1-AC-004 | 以人工标注为准，至少 90% 的自动句段边界误差不超过 500 毫秒。 |
| P1-AC-005 | 用户无需操作波形，只通过点击识别文字即可为指定句段完成局部重录和版本选择。 |
| P1-AC-006 | 对一段至少包含 10 个句段的音频，合并两个相邻句段后再拆分，文字、播放范围和顺序一致，源录音校验值不变。 |
| P1-AC-007 | 为中间句段录制两个不同时长的版本。选用第二版后只替换目标句，前后内容完整并自动顺延；恢复源录音后全文回到原始内容。 |
| P1-AC-008 | 替换三个不相邻句段并导出，结果无句段遗漏、重复或顺序错误。 |
| P1-AC-009 | 抽查全部替换点，全文预览与导出的采用版本、顺序和停顿参数一致。 |
| P1-AC-010 | 强制结束应用后重新进入，识别文本、句段边界、全部录音版本、当前采用版本和上次播放位置均能恢复。 |
| P1-AC-011 | 飞行模式下完成录制或导入、端侧识别、分句、局部重录、全文预览和本地导出，网络日志中无数据外发请求。 |
| P1-AC-012 | 分别模拟损坏文件、无有效语音、录制中断和导出中断，源录音及最近一次有效编辑状态保持可用。 |
| P1-AC-013 | 构造低于审核阈值、置信度不可用和审核阈值缺失三种识别结果；对应句段均进入待人工审核，审核未完成时导出返回明确错误且不生成 `PUBLISHED` 记录；用户确认当前文字和边界后，且文字、边界和审核策略版本未变化，导出成功。修改已审核句段文字或边界，或升级审核策略版本后，审核状态重新变为待审核。 |

性能目标：参考设备处理五分钟普通话录音的转写与分句时间不超过 60 秒，并持续显示处理进度。该目标在端侧模型确定后通过实机基准测试复核。

### 2.10 首版范围

首版交付范围包括：单说话人普通话完整录制、常用本地音频导入、可选提纲、端侧 ASR、自动分句、文本修订、句段合并与拆分、边界调整、逐句多版本补录、上下文试听、非破坏式局部替换、全文预览、项目恢复和一种通用格式导出。首版性能验证以 20 分钟以内音频为范围。

后续版本可扩展多说话人分离、背景音乐多轨处理、更多语言、自动改稿和跨设备协作。

## 3. 非功能需求

| 编号 | 类别 | 要求 |
| --- | --- | --- |
| P1-NFR-001 | 离线能力 | 应用安装完成后，录音、音频导入、端侧识别、分句、局部重录、预览和导出均不依赖网络。 |
| P1-NFR-002 | 数据隐私 | 音频、提纲、转写文本、模型中间结果和编辑记录保存在应用私有目录；只有用户主动导出或分享时，指定文件才离开应用。 |
| P1-NFR-003 | 源素材保护 | 编辑过程不覆盖源录音。应用内部副本与用户选择的原文件分开管理。 |
| P1-NFR-004 | 删除 | 删除项目时，同步删除应用私有目录内的源录音副本、替换录音、缓存、转写结果和编辑记录。 |
| P1-NFR-005 | 可靠性 | 项目状态采用原子写入；保存失败时保留上一个有效版本；临时导出文件与正式结果分开。 |
| P1-NFR-006 | 可恢复性 | 应用异常退出后恢复最后一次已确认状态；已经写入的未完成录音作为待确认草稿恢复。 |
| P1-NFR-007 | 响应反馈 | 普通按钮操作在 100 毫秒内出现视觉反馈；耗时超过一秒的处理显示进度，并提供安全退出入口。 |
| P1-NFR-008 | 模型一致性 | 项目记录 ASR 模型版本；识别完成后保存结构化结果，应用重启不重复识别。 |
| P1-NFR-009 | 易用性 | 主要操作支持单手完成；“重录”为编辑页面主按钮，触控区域不小于 48vp。 |
| P1-NFR-010 | 无障碍 | 文字跟随系统字号，按钮具备无障碍标签，句段状态同时使用文字和视觉标记表达。 |
| P1-NFR-011 | 资源管理 | 模型体积、峰值内存、最低设备配置和存储需求在模型选型后实测并写入兼容设备说明。 |
| P1-NFR-012 | 可维护性 | 项目数据包含模式版本号；应用或模型升级后能够迁移既有项目，迁移失败时保留原数据副本。 |

## 4. 需求追踪关系

| 用户故事 | 主要功能需求 | 主要验收用例 |
| --- | --- | --- |
| P1-US-01 完整录音后改一句 | P1-FR-002、006—008、011—017 | P1-AC-005、007—009 |
| P1-US-02 只有音频 | P1-FR-004、006—009 | P1-AC-001、002 |
| P1-US-03 导入已有音频 | P1-FR-003、006—009、018 | P1-AC-001、008、011 |
| P1-US-04 修正识别错误 | P1-FR-009、010、020 | P1-AC-004、006、012 |
| P1-US-05 多版本比较 | P1-FR-012—017、021 | P1-AC-007—010 |

## 5. 项目风险与需求对策

| 风险 | 影响 | 需求对策 |
| --- | --- | --- |
| 噪声、口音和专业词导致识别错误 | 用户难以通过文字找到目标声音 | 允许改字、试听、合并、拆分和调整边界；可选提纲提供专业词辅助。 |
| 补录环境改变 | 新旧句的音量和听感不一致 | 提供上下文回听、多版本比较、响度匹配和短淡入淡出。 |
| 替换时长变化 | 后续声音位置发生变化 | 所有原句段引用不可变的源录音时间范围，成片通过编辑决策重新组合。 |
| 端侧 ASR 模型体积或速度不满足目标 | 包体过大或等待时间过长 | 模型选型阶段确定量化规格与最低设备，在参考设备上完成基准测试。 |
| 模型、推理框架或编解码组件授权不清晰 | 影响应用市场上架 | 技术选型阶段记录许可证、商用范围和分发条件。 |

## 6. 首版交付物

1. 可在目标鸿蒙手机安装并离线运行的应用包；
2. 覆盖完整录音、端侧识别、点句重录和成片导出的演示素材；
3. 与 P1-AC 编号对应的功能、识别准确率、句段边界和性能测试记录；
4. ASR 模型、音频格式、权限、编辑决策与本地存储说明；
5. 真实用户完成局部修改任务的测试记录；
6. 源代码说明、构建文档和使用文档。

本文 V1.1 为项目一的需求基线。后续变更应记录变更原因、受影响需求编号、验收标准变化及文档版本。
